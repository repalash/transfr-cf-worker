{
	"name": "transfr-cf-worker",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"description": "Dead-simple, encrypted, temporary file transfer server on Cloudflare Workers + R2",
	"license": "MIT",
	"scripts": {
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"deploy": "wrangler deploy",
		"test": "vitest run",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.22.0",
		"@cloudflare/workers-types": "^5.20260921.1",
		"@types/node": "^26.6.2",
		"typescript": "^5.9.0",
		"vitest": "^4.1.0",
		"wrangler": "^4.136.0"
	}
}
